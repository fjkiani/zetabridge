{
  "name": "@workspace/route-policy",
  "version": "0.0.0",
  "private": true,
  "description": "STC-1010 ModelRouter anti-EGFR route policy (RUO - research use only; not for clinical care).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/routePolicy.test.js",
    "emit-payload": "python3 emit_model_router_payload.py"
  },
  "devDependencies": {
    "@types/node": "catalog:"
  }
}
