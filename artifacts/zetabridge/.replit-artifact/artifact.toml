kind = "web"
previewPath = "/"
title = "ZetaBridge Dashboard"
version = "1.0.0"
id = "artifacts/zetabridge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24052

[services.development]
run = "pnpm --filter @workspace/zetabridge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/zetabridge", "run", "build" ]
publicDir = "artifacts/zetabridge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24052"
BASE_PATH = "/"
