# receipts/

Drop tool envelope JSON here (e.g. `{tool_id}.{timestamp}.envelope.json`).

Large matrices / CSVs: write elsewhere (`datasets/genie_r20/mcp_receipts/`) and only store sha + path in the envelope / Brenus METRICS_LEDGER.
